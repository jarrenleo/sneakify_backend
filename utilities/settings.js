/**
 * Locale identifiers mapped to each supported country code.
 * @type {Object.<string, string>}
 * @property {string} SG - Locale for Singapore.
 * @property {string} MY - Locale for Malaysia.
 * @property {string} JP - Locale for Japan.
 * @property {string} GB - Locale for United Kingdom.
 * @property {string} NL - Locale for Netherlands.
 * @property {string} AU - Locale for Australia.
 * @property {string} CA - Locale for Canada.
 * @property {string} US - Locale for United States.
 */
export const locales = {
  SG: "en-SG",
  MY: "en-MY",
  JP: "ja-JP",
  GB: "en-GB",
  NL: "nl-NL",
  AU: "en-AU",
  CA: "en-CA",
  US: "en-US",
};

/**
 * Currency codes mapped to each supported country code.
 * @type {Object.<string, string>}
 * @property {string} SG - Currency for Singapore: Singapore Dollar (SGD).
 * @property {string} MY - Currency for Malaysia: Malaysian Ringgit (MYR).
 * @property {string} JP - Currency for Japan: Japanese Yen (JPY).
 * @property {string} GB - Currency for United Kingdom: British Pound (GBP).
 * @property {string} NL - Currency for Netherlands: Euro (EUR).
 * @property {string} AU - Currency for Australia: Australian Dollar (AUD).
 * @property {string} CA - Currency for Canada: Canadian Dollar (CAD).
 * @property {string} US - Currency for United States: United States Dollar (USD).
 */
export const currencies = {
  SG: "SGD",
  MY: "MYR",
  JP: "JPY",
  GB: "GBP",
  NL: "EUR",
  AU: "AUD",
  CA: "CAD",
  US: "USD",
};
